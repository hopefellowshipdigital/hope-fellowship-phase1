import type { SermonSummary } from "@/types";

// No real sermon has been configured yet. This stays null rather than
// showing invented bracket placeholders — the Latest Message section
// shows a neutral, visitor-friendly panel instead until a real sermon
// record is added here.
export const featuredSermon: SermonSummary | null = null;
